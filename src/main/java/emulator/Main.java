package emulator;

import java.util.ArrayList;

import emulator.op.Instruction;

public class Main {
    public static void main(String[] args) {
        ArrayList<Instruction> prog = new ArrayList<>();

        Emulator emulator = new Emulator(null, 0, 0)
    }
}
